[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tapcore"
version = "0.1.0"
description = "Shared core for the SKAO TAP microservices: ADQL translation, typed streaming result serialization, UWS job model"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary,pool]>=3.1",
    "pyarrow>=17.0",
    "queryparser-python3>=0.7",
]

[tool.setuptools.packages.find]
include = ["tapcore*"]
