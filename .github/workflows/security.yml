name: Security

# NOTE: this repository is currently private. CodeQL, dependency review and
# SARIF upload to code scanning require GitHub Advanced Security on private
# repositories, so those jobs are gated: they run automatically when the
# repository is public, or when a repository variable ENABLE_GHAS=true is set
# after enabling Advanced Security. pip-audit and the Trivy scan itself run
# regardless.

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 5 * * 1"  # weekly, Monday morning UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  codeql:
    name: CodeQL (Python)
    if: ${{ !github.event.repository.private || vars.ENABLE_GHAS == 'true' }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v7
      - uses: github/codeql-action/init@v3
        with:
          languages: python
          queries: security-and-quality
      - uses: github/codeql-action/analyze@v3
        with:
          category: codeql-python

  dependency-review:
    name: Dependency review
    if: ${{ github.event_name == 'pull_request' && (!github.event.repository.private || vars.ENABLE_GHAS == 'true') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: high

  pip-audit:
    name: pip-audit (known vulnerabilities)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      # match requires-python: the lock only carries hashes for wheels
      # relevant to >=3.14, so pip-audit must resolve under 3.14 too
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Export locked requirements
        # git-pinned packages are excluded: pip-audit's hash-checked dry-run
        # cannot resolve them (their own deps stay in the export)
        run: uv export --frozen --no-dev --no-emit-project --no-emit-workspace --no-emit-package ska-src-mm-notification --no-emit-package ska-src-sdm -o requirements.txt
      - uses: pypa/gh-action-pip-audit@v1.1.0
        with:
          inputs: requirements.txt

  trivy:
    name: Trivy scan (vuln, secrets, misconfig)
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v7
      - name: Scan (results in job log; fails on CRITICAL)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scanners: vuln,secret,misconfig
          format: table
          severity: CRITICAL,HIGH
          exit-code: "1"
      - name: Scan to SARIF
        if: ${{ always() && (!github.event.repository.private || vars.ENABLE_GHAS == 'true') }}
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scanners: vuln,secret,misconfig
          format: sarif
          output: trivy.sarif
          severity: CRITICAL,HIGH
      - name: Upload SARIF to code scanning
        if: ${{ always() && (!github.event.repository.private || vars.ENABLE_GHAS == 'true') }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy.sarif
          category: trivy-fs
